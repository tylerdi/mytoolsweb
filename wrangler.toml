# Cloudflare Pages 配置文件
name = "mytoolsweb"
compatibility_date = "2024-03-12"
pages_build_output_dir = "."

# 环境变量全部在 Cloudflare Dashboard 管理
# Dashboard → Pages → mytoolsweb → Settings → Environment variables

# Functions 配置
[functions]
node_compat = true
